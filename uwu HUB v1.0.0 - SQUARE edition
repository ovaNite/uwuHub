local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- ⚙️ INITIAL SETTINGS
local Settings = {
    AimbotEnabled = false,
    AimbotType = "Camera",
    TeamCheck = true,
    Smoothness = 0.15,
    Range = 200, -- FOV Radius
    VisualRange = 500, -- ESP/Charms Reichweite
    AimKey = Enum.KeyCode.E, 
    ToggleKey = Enum.KeyCode.RightShift,
    TargetPart = "Head",
    UIVisible = true,
    CharmsEnabled = false,
    ESPEnabled = false,
    TracersEnabled = false,
    FOVVisible = false,
    FOVThickness = 0.2,
    FOVColor = Color3.fromRGB(255, 0, 0)
}

-- 💾 CONFIG SYSTEM
local ConfigFile = "uwuHub_SquareConfig.json"
local function SaveConfig()
    local Data = {
        AimbotType = Settings.AimbotType, TeamCheck = Settings.TeamCheck,
        Smoothness = Settings.Smoothness, Range = Settings.Range,
        VisualRange = Settings.VisualRange, AimKey = tostring(Settings.AimKey),
        TargetPart = Settings.TargetPart, CharmsEnabled = Settings.CharmsEnabled,
        ESPEnabled = Settings.ESPEnabled, TracersEnabled = Settings.TracersEnabled,
        FOVVisible = Settings.FOVVisible
    }
    if writefile then writefile(ConfigFile, HttpService:JSONEncode(Data)) end
end

local function LoadConfig()
    if isfile and isfile(ConfigFile) then
        local Data = HttpService:JSONDecode(readfile(ConfigFile))
        for i, v in pairs(Data) do
            if i == "AimKey" then
                Settings.AimKey = Enum.KeyCode[v:gsub("Enum.KeyCode.", "")] or Enum.UserInputType[v:gsub("Enum.UserInputType.", "")]
            else Settings[i] = v end
        end
    end
end
LoadConfig()

-- 🎨 UI BASE (ECKIGES DESIGN)
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "uwuHub_SquareEdition"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 550, 0, 420)
MainFrame.Position = UDim2.new(0.5, -275, 0.5, -210)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 1
MainFrame.BorderColor3 = Color3.fromRGB(40, 40, 40)
MainFrame.Active = true

-- 🖱️ DRAG STRIP (OBEN)
local DragStrip = Instance.new("Frame", MainFrame)
DragStrip.Size = UDim2.new(1, 0, 0, 25)
DragStrip.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
DragStrip.BorderSizePixel = 1
DragStrip.BorderColor3 = Color3.fromRGB(45, 45, 45)

local Title = Instance.new("TextLabel", DragStrip)
Title.Text = "  uwu HUB v1.0.0 - SQUARE EDITION"
Title.Size = UDim2.new(1, -40, 1, 0)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.Code
Title.TextSize = 14
Title.TextXAlignment = "Left"

local CloseBtn = Instance.new("TextButton", DragStrip)
CloseBtn.Text = "X"
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.Position = UDim2.new(1, -25, 0, 0)
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
CloseBtn.BorderSizePixel = 0
CloseBtn.TextColor3 = Color3.new(1, 1, 1)

-- DRAG LOGIC
local dragging, dragStart, startPos
DragStrip.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true dragStart = input.Position startPos = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)

-- SIDEBAR (TABS)
local SideBar = Instance.new("Frame", MainFrame)
SideBar.Size = UDim2.new(0, 120, 1, -25)
SideBar.Position = UDim2.new(0, 0, 0, 25)
SideBar.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
SideBar.BorderSizePixel = 1
SideBar.BorderColor3 = Color3.fromRGB(40, 40, 40)

-- PAGE CONTAINER
local function CreatePage()
    local f = Instance.new("ScrollingFrame", MainFrame)
    f.Size = UDim2.new(1, -135, 1, -40)
    f.Position = UDim2.new(0, 130, 0, 35)
    f.BackgroundTransparency = 1
    f.CanvasSize = UDim2.new(0, 0, 1.5, 0)
    f.ScrollBarThickness = 0
    f.Visible = false
    return f
end

local MainContent = CreatePage() MainContent.Visible = true
local ESPContent = CreatePage()
local FOVContent = CreatePage()
local SettingsContent = CreatePage()

local function AddTab(name, pos, page)
    local btn = Instance.new("TextButton", SideBar)
    btn.Text = name btn.Size = UDim2.new(1, 0, 0, 40)
    btn.Position = UDim2.new(0, 0, 0, pos)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    btn.BorderSizePixel = 1
    btn.BorderColor3 = Color3.fromRGB(40, 40, 40)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.Code
    btn.MouseButton1Click:Connect(function()
        MainContent.Visible = false ESPContent.Visible = false FOVContent.Visible = false SettingsContent.Visible = false
        page.Visible = true
    end)
end
AddTab("COMBAT", 0, MainContent)
AddTab("VISUALS", 40, ESPContent)
AddTab("FOV", 80, FOVContent)
AddTab("SETTINGS", 120, SettingsContent)

-- SLIDER COMPONENT (ECKIG)
local function CreateSlider(parent, text, pos, startVal, max, callback)
    local label = Instance.new("TextLabel", parent)
    label.Text = text..": "..startVal
    label.Position = UDim2.new(0, 5, 0, pos)
    label.Size = UDim2.new(0, 200, 0, 20)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Font = Enum.Font.Code
    label.TextXAlignment = "Left"

    local bg = Instance.new("Frame", parent)
    bg.Size = UDim2.new(0, 380, 0, 12)
    bg.Position = UDim2.new(0, 5, 0, pos + 25)
    bg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    bg.BorderSizePixel = 1
    bg.BorderColor3 = Color3.fromRGB(60, 60, 60)

    local fill = Instance.new("Frame", bg)
    fill.Size = UDim2.new(startVal/max, 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- Rot als Akzent
    fill.BorderSizePixel = 0

    local dragging = false
    bg.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true end end)
    UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)
    
    RunService.RenderStepped:Connect(function()
        if dragging then
            local mx = UserInputService:GetMouseLocation().X
            local p = math.clamp((mx - bg.AbsolutePosition.X) / bg.AbsoluteSize.X, 0, 1)
            local val = (max > 1) and math.floor(p * max) or string.format("%.2f", p)
            fill.Size = UDim2.new(p, 0, 1, 0)
            label.Text = text..": "..val
            callback(tonumber(val))
        end
    end)
end

--- 🔵 COMBAT PAGE
local AimToggle = Instance.new("TextButton", MainContent)
AimToggle.Size = UDim2.new(0,180,0,35) AimToggle.Position = UDim2.new(0,5,0,5)
AimToggle.Text = "Aimbot: AUS" AimToggle.BackgroundColor3 = Color3.fromRGB(150,50,50)
AimToggle.TextColor3 = Color3.new(1,1,1) AimToggle.BorderSizePixel = 1

local TypeBtn = Instance.new("TextButton", MainContent)
TypeBtn.Size = UDim2.new(0,180,0,35) TypeBtn.Position = UDim2.new(0,5,0,50)
TypeBtn.Text = "Type: "..Settings.AimbotType TypeBtn.BackgroundColor3 = Color3.fromRGB(35,35,35)
TypeBtn.TextColor3 = Color3.new(1,1,1) TypeBtn.BorderSizePixel = 1

local KeybindBtn = Instance.new("TextButton", MainContent)
KeybindBtn.Size = UDim2.new(0,180,0,35) KeybindBtn.Position = UDim2.new(0,200,0,50)
KeybindBtn.Text = "Key: "..Settings.AimKey.Name KeybindBtn.BackgroundColor3 = Color3.fromRGB(35,35,35)
KeybindBtn.TextColor3 = Color3.new(1,1,1) KeybindBtn.BorderSizePixel = 1

local TeamToggle = Instance.new("TextButton", MainContent)
TeamToggle.Size = UDim2.new(0,180,0,35) TeamToggle.Position = UDim2.new(0,5,0,95)
TeamToggle.Text = "Teamcheck: AN" TeamToggle.BackgroundColor3 = Color3.fromRGB(50,150,50)
TeamToggle.TextColor3 = Color3.new(1,1,1) TeamToggle.BorderSizePixel = 1

local PartBtn = Instance.new("TextButton", MainContent)
PartBtn.Size = UDim2.new(0,180,0,35) PartBtn.Position = UDim2.new(0,200,0,95)
PartBtn.Text = "Target: "..Settings.TargetPart PartBtn.BackgroundColor3 = Color3.fromRGB(35,35,35)
PartBtn.TextColor3 = Color3.new(1,1,1) PartBtn.BorderSizePixel = 1

CreateSlider(MainContent, "Smoothness", 145, Settings.Smoothness, 1, function(v) Settings.Smoothness = v end)

--- 🟡 VISUALS PAGE
local function NewESP(txt, pos, callback, startState)
    local btn = Instance.new("TextButton", ESPContent)
    btn.Text = txt..": AUS" btn.Size = UDim2.new(0,180,0,35) btn.Position = pos
    btn.BackgroundColor3 = Color3.fromRGB(150,50,50) btn.TextColor3 = Color3.new(1,1,1)
    btn.BorderSizePixel = 1
    local s = startState or false
    local function update()
        btn.Text = txt..(s and ": AN" or ": AUS")
        btn.BackgroundColor3 = s and Color3.fromRGB(50,150,50) or Color3.fromRGB(150,50,50)
    end
    btn.MouseButton1Click:Connect(function() s = not s update() callback(s) end)
    update()
end
NewESP("Charms", UDim2.new(0,5,0,5), function(v) Settings.CharmsEnabled = v end, Settings.CharmsEnabled)
NewESP("Box", UDim2.new(0,200,0,5), function(v) Settings.ESPEnabled = v end, Settings.ESPEnabled)
NewESP("Tracers", UDim2.new(0,5,0,50), function(v) Settings.TracersEnabled = v end, Settings.TracersEnabled)

CreateSlider(ESPContent, "Visuals Range", 100, Settings.VisualRange, 2000, function(v) Settings.VisualRange = v end)

--- 🔴 FOV PAGE
local FOVToggle = Instance.new("TextButton", FOVContent)
FOVToggle.Size = UDim2.new(0,180,0,35) FOVToggle.Position = UDim2.new(0,5,0,5)
FOVToggle.Text = "FOV Circle: AUS" FOVToggle.BackgroundColor3 = Color3.fromRGB(150,50,50)
FOVToggle.TextColor3 = Color3.new(1,1,1) FOVToggle.BorderSizePixel = 1

CreateSlider(FOVContent, "FOV Radius", 55, Settings.Range, 500, function(v) Settings.Range = v end)

--- 💾 SETTINGS PAGE
local SaveBtn = Instance.new("TextButton", SettingsContent)
SaveBtn.Size = UDim2.new(0,180,0,40) SaveBtn.Position = UDim2.new(0,5,0,5)
SaveBtn.Text = "SAVE CONFIG" SaveBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 150)
SaveBtn.TextColor3 = Color3.new(1,1,1) SaveBtn.Font = "Code"
SaveBtn.MouseButton1Click:Connect(function() SaveConfig() SaveBtn.Text = "SAVED!" wait(1) SaveBtn.Text = "SAVE CONFIG" end)

--- ⚙️ ENGINE (LOGIC)
local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Settings.FOVColor
FOVCircle.Thickness = Settings.FOVThickness
FOVCircle.Visible = false

RunService.RenderStepped:Connect(function()
    FOVCircle.Position = UserInputService:GetMouseLocation()
    FOVCircle.Radius = Settings.Range
    
    local IsPressed = (Settings.AimKey.EnumType == Enum.KeyCode and UserInputService:IsKeyDown(Settings.AimKey)) or (Settings.AimKey.EnumType == Enum.UserInputType and UserInputService:IsMouseButtonPressed(Settings.AimKey))
    if Settings.AimbotEnabled and IsPressed then
        local Target, MouseDist = nil, Settings.Range
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild(Settings.TargetPart) then
                if not Settings.TeamCheck or p.Team ~= LocalPlayer.Team then
                    local Pos, OnScreen = Camera:WorldToViewportPoint(p.Character[Settings.TargetPart].Position)
                    local D = (Vector2.new(Pos.X, Pos.Y) - UserInputService:GetMouseLocation()).Magnitude
                    if OnScreen and D < MouseDist then Target = p.Character[Settings.TargetPart] MouseDist = D end
                end
            end
        end
        if Target then
            if Settings.AimbotType == "Camera" then
                Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, Target.Position), Settings.Smoothness)
            else
                local Pos, _ = Camera:WorldToViewportPoint(Target.Position)
                local MousePos = UserInputService:GetMouseLocation()
                mousemoverel((Pos.X - MousePos.X) * Settings.Smoothness, (Pos.Y - MousePos.Y) * Settings.Smoothness)
            end
        end
    end
end)

-- Buttons Connects
TypeBtn.MouseButton1Click:Connect(function() Settings.AimbotType = (Settings.AimbotType == "Camera") and "Mouse" or "Camera" TypeBtn.Text = "Type: "..Settings.AimbotType end)
PartBtn.MouseButton1Click:Connect(function() Settings.TargetPart = (Settings.TargetPart == "Head") and "HumanoidRootPart" or "Head" PartBtn.Text = "Target: "..Settings.TargetPart end)
TeamToggle.MouseButton1Click:Connect(function() Settings.TeamCheck = not Settings.TeamCheck TeamToggle.Text = "Teamcheck: "..(Settings.TeamCheck and "AN" or "AUS") TeamToggle.BackgroundColor3 = Settings.TeamCheck and Color3.fromRGB(50,150,50) or Color3.fromRGB(150,50,50) end)
AimToggle.MouseButton1Click:Connect(function() Settings.AimbotEnabled = not Settings.AimbotEnabled AimToggle.Text = "Aimbot: "..(Settings.AimbotEnabled and "AN" or "AUS") AimToggle.BackgroundColor3 = Settings.AimbotEnabled and Color3.fromRGB(50,150,50) or Color3.fromRGB(150,50,50) end)
FOVToggle.MouseButton1Click:Connect(function() Settings.FOVVisible = not Settings.FOVVisible FOVToggle.Text = "FOV Circle: "..(Settings.FOVVisible and "AN" or "AUS") FOVCircle.Visible = Settings.FOVVisible end)

local Waiting = false
KeybindBtn.MouseButton1Click:Connect(function() Waiting = true KeybindBtn.Text = "..." end)
UserInputService.InputBegan:Connect(function(i, p)
    if Waiting then
        if i.UserInputType == Enum.UserInputType.Keyboard then Settings.AimKey = i.KeyCode KeybindBtn.Text = "Key: "..i.KeyCode.Name Waiting = false
        elseif i.UserInputType == Enum.UserInputType.MouseButton2 then Settings.AimKey = Enum.UserInputType.MouseButton2 KeybindBtn.Text = "Key: Mouse2" Waiting = false end
    elseif not p and i.KeyCode == Settings.ToggleKey then MainFrame.Visible = not MainFrame.Visible end
end)

local function CreateESP(Player)
    local Line = Drawing.new("Line") Line.Color = Color3.fromRGB(255, 0, 0)
    local Box = Drawing.new("Square")
    RunService.RenderStepped:Connect(function()
        if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") and Player ~= LocalPlayer then
            local isEnemy = (not Settings.TeamCheck or Player.Team ~= LocalPlayer.Team)
            local root = Player.Character.HumanoidRootPart
            local dist = (root.Position - Camera.CFrame.Position).Magnitude
            local Pos, OnScreen = Camera:WorldToViewportPoint(root.Position)
            local highlight = Player.Character:FindFirstChild("uwuCharms")

            if Settings.CharmsEnabled and isEnemy and dist <= Settings.VisualRange then
                if not highlight then highlight = Instance.new("Highlight", Player.Character) highlight.Name = "uwuCharms" end
                highlight.Enabled = true
            elseif highlight then highlight.Enabled = false end
            if Settings.TracersEnabled and OnScreen and isEnemy and dist <= Settings.VisualRange then
                Line.Visible = true Line.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2) Line.To = Vector2.new(Pos.X, Pos.Y)
            else Line.Visible = false end
            if Settings.ESPEnabled and OnScreen and isEnemy and dist <= Settings.VisualRange then
                Box.Visible = true Box.Size = Vector2.new(2000/Pos.Z, 2500/Pos.Z) Box.Position = Vector2.new(Pos.X-Box.Size.X/2, Pos.Y-Box.Size.Y/2)
            else Box.Visible = false end
        else Line.Visible = false Box.Visible = false end
    end)
end
for _, p in pairs(Players:GetPlayers()) do CreateESP(p) end
Players.PlayerAdded:Connect(CreateESP)
CloseBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false end)

